import { createMuiTheme } from "@material-ui/core/styles";
//import { blue, orange } from "@material-ui/core/colors/red";

export const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#F4511E",
    },
    secondary: {
      main: "#ff9100",
    },
  },
});
